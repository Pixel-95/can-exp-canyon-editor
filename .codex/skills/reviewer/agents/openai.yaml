interface:
  display_name: "Reviewer"
  short_description: "Review changes for bugs, regressions, and test gaps"
  default_prompt: "Use $reviewer to perform a findings-first review of the current changes."
policy:
  allow_implicit_invocation: true
